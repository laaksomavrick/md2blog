<!DOCTYPE html>
<html>

    <%- include('./head.ejs', {title: title}); %>

    <body>
        <% if (title) { %>
        <h1>Hello, <%= title %></h1>
        <% } else { %>
        <h1> Hello, world </h1>
        <% } %>

        <% for (const post of required.posts) { %>
        <h1><%= post.title %></h1>
        <% } %>

        <%- content %>
    </body>

</html>