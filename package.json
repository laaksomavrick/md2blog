{
    "name": "static-site-generator",
    "version": "0.1.0",
    "description": "A static site generator",
    "license": "MIT",
    "engines": {
        "node": "10"
    },
    "scripts": {
        "generate": "ts-node cmd/index.ts",
        "format": "prettier --write \"cmd/**/*.ts\" \"lib/**/*.ts\""
    },
    "devDependencies": {
        "prettier": "^1.18.2",
        "ts-node": "^8.3.0",
        "tslint": "^5.18.0",
        "typescript": "^3.5.3"
    }
}
